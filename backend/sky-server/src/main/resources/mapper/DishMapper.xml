<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.DishMapper">

    <!--  菜品分页查询  -->
    <select id="pageQuery" resultType="com.sky.vo.DishVO">
        select d.*, c.name as category_name
        from dish d
        left join category c on d.category_id = c.id
        <where>
            <if test="name != null and name != ''">
                and d.name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and d.category_id = #{categoryId}
            </if>
            <if test="status != null">
                and d.status = #{status}
            </if>
            <if test="storeId != null">
                and d.store_id = #{storeId}
            </if>
        </where>
        order by d.create_time desc
    </select>

    <!--  根据菜品id集合统计处于指定状态的菜品数量  -->
    <select id="countByStatus" resultType="java.lang.Integer">
        select count(id) from dish
        where status = #{status}
        <if test="ids != null and ids.size() > 0">
            and id in
            <foreach collection="ids" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
    </select>

    <!--  根据菜品id集合批量删除菜品  -->
    <delete id="deleteByIds">
        delete from dish where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <if test="storeId != null">
            and store_id = #{storeId}
        </if>
    </delete>

    <!--  修改菜品  -->
    <update id="update" parameterType="com.sky.entity.Dish">
        update dish
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="image != null">image = #{image},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        where id = #{id}
        <if test="storeId != null">
            and store_id = #{storeId}
        </if>
    </update>

    <!--  根据分类id查询在售菜品列表  -->
    <select id="listByCategoryId" resultType="com.sky.entity.Dish">
        select * from dish
        where status = 1 and category_id = #{categoryId}
        order by create_time desc
    </select>

    <!--  新增菜品  -->
    <insert id="save" parameterType="com.sky.entity.Dish" useGeneratedKeys="true" keyProperty="id">
        insert into dish (name, category_id, price, image, description, status, store_id, create_time, update_time, create_user, update_user)
        values (#{name}, #{categoryId}, #{price}, #{image}, #{description}, #{status}, #{storeId}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <!--  管理端：根据分类id查询该分类全部菜品（含停售）  -->
    <select id="listByCategoryAll" resultType="com.sky.entity.Dish">
        select * from dish
        where category_id = #{categoryId}
        <if test="storeId != null">
            and store_id = #{storeId}
        </if>
        order by create_time desc
    </select>
</mapper>