<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd" >
<mapper namespace="com.sky.mapper.SetmealMapper">

    <!--  新增套餐  -->
    <insert id="save" parameterType="com.sky.entity.Setmeal" useGeneratedKeys="true" keyProperty="id">
        insert into setmeal (category_id, name, price, status, description, image, store_id, create_time, update_time, create_user, update_user)
        values (#{categoryId}, #{name}, #{price}, #{status}, #{description}, #{image}, #{storeId}, #{createTime}, #{updateTime}, #{createUser}, #{updateUser})
    </insert>

    <!--  根据套餐id集合统计处于指定状态的套餐数量  -->
    <select id="countByStatus" resultType="java.lang.Integer">
        select count(id) from setmeal
        where status = #{status}
        <if test="ids != null and ids.size() > 0">
            and id in
            <foreach collection="ids" item="id" open="(" separator="," close=")">
                #{id}
            </foreach>
        </if>
    </select>

    <!--  套餐分页查询  -->
    <select id="pageQuery" resultType="com.sky.vo.SetmealVO">
        select s.*, c.name as category_name
        from setmeal s
        left join category c on s.category_id = c.id
        <where>
            <if test="name != null and name != ''">
                and s.name like concat('%',#{name},'%')
            </if>
            <if test="categoryId != null">
                and s.category_id = #{categoryId}
            </if>
            <if test="status != null">
                and s.status = #{status}
            </if>
            <if test="storeId != null">
                and s.store_id = #{storeId}
            </if>
        </where>
        order by s.create_time desc
    </select>

    <!--  修改套餐  -->
    <update id="update" parameterType="com.sky.entity.Setmeal">
        update setmeal
        <set>
            <if test="name != null">name = #{name},</if>
            <if test="categoryId != null">category_id = #{categoryId},</if>
            <if test="price != null">price = #{price},</if>
            <if test="image != null">image = #{image},</if>
            <if test="description != null">description = #{description},</if>
            <if test="status != null">status = #{status},</if>
            <if test="updateTime != null">update_time = #{updateTime},</if>
            <if test="updateUser != null">update_user = #{updateUser},</if>
        </set>
        where id = #{id}
        <if test="storeId != null">
            and store_id = #{storeId}
        </if>
    </update>

    <!--  根据套餐id集合批量删除套餐  -->
    <delete id="deleteByIds">
        delete from setmeal where id in
        <foreach collection="ids" item="id" open="(" separator="," close=")">
            #{id}
        </foreach>
        <if test="storeId != null">
            and store_id = #{storeId}
        </if>
    </delete>
</mapper>